{# @block content The default block #}
<ul {{ attributes.without('class') }}
    class="{{ 'flex flex-row items-center list-none gap-1 ' ~ attributes.render('class')|tailwind_merge }}"
>
    {%- block content %}{% endblock -%}
</ul>
